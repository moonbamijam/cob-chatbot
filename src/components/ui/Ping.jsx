import React from "react";

// sample ping when the bot messages
const Ping = () => {
  return (
    <div className="absolute right-1 -top-1 animate-ping w-4 h-4 rounded-full bg-red-500"></div>
  );
};

export default Ping;
